---
description: Export levels from levels.numbers → individual CSV files
allowed-tools: Bash(npm run export-levels:*)
---

Run `npm run export-levels` to export each sheet from the Numbers file as an individual CSV file in `src/levels/`, then report how many levels were written.
